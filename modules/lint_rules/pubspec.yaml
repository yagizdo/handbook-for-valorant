name: lint_rules
description: Custom lint rules for BaseCubit architecture patterns.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: ">=8.4.0 <9.0.0"
  custom_lint_builder: 0.8.1
