/// Enum representing available theme types in the app.
///
/// Currently supports [light] and [dark] modes.
/// Extensible for future theme variations (e.g., blue, green schemes).
enum ThemeType { light, dark }
