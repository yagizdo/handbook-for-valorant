import 'dart:ui';

class WeaponDamageColors {
  WeaponDamageColors._();

  /// Body damage bar color — amber/orange
  static const Color bodyDamage = Color(0xFFE8A83E);

  /// Leg damage bar color — muted gray
  static const Color legDamage = Color(0xFF6B7280);
}
