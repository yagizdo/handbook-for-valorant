import 'package:flutter/material.dart';

class MapsDetail extends StatelessWidget {
  const MapsDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
